#include "main.h"
#include <ctype.h>

/**
 * _isupper - check a character is uppercase
 * @c: character
 * Return: 1 upper 0  lower
 */
int _isupper(int j)
{
	if (isupper(j))
	{
		return (1);
	}
	return (0);
}
